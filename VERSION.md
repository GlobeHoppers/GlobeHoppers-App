# JourneyLines v2.19 — Space Star + Rim Polish

- Replaces the uniform repeating star-field with a non-repeating, more organic star layout.
- Slows the stylized place pin drop animation by roughly 50%+ for a more graceful arrival.
- Removes the viewport-centered halo layer that looked like it was centered on the camera.
- Relies more on MapLibre globe fog/atmosphere for the horizon/rim glow so the glow follows the globe edge.
- Keeps v2.18 tile-preload, private Mapbox build-time route cache, and space-globe behavior.
